
#include <iostream>
#include <windows.h>
#include "testlist.hpp"
#include "testtimer.hpp"
#include <memory>
#include "headers.h"
#include <string.h>

using namespace std;


int main()
{
    //testList1();
  /*  uint64 a = (uint64(1) << 64) - 1;
	cout << a << endl;
	cout << a / 1000 / 24 / 60 / 60 / 365 << endl;*/
	//testTimer();
    //char* c = new char;
    //char** d = new char*[10];
    //memset(d, 0, sizeof(char) * 10);
    ////auto e = d + 1;
    //int32* a = new int32[10];
    //memset(a, 0, sizeof(int32) * 10);
    //cout << c + 1 << "   " << sizeof(void*) << endl;;
    //int32 a[10];
    //for (auto i = 0; i < 10; i++)
    //{
    //    a[i] = i;
    //}
    //int32 b[10];
    //memset(b, 0, sizeof(int32) * 10);
    //memcpy(b, a, sizeof(int32) * 10);
    //for (int i =0 ; i < 10 ;i ++)
    //{
    //    cout << b[i] << endl;
    //    cout << a[i] << endl;
    //}
    //cout << endl;
    //cout << sizeof(char*) << endl;
    bf::testbuff();
    system("pause");
    return 0;
}

